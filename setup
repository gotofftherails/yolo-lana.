==The tools and utilities you need==

Ensure Python and Python tools are deployed.  The means easy_install is in place.


$ sudo apt-get install python-setuptools

Use easy install to install BeautifulSoup

$ easy_install beautiful soup

Install SPLAT!

$ apt-get install splat

# for f in *.hgt ; do srtm2sdf "$f" ; done
